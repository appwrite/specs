```graphql
mutation {
    accountCreateMFAAuthenticator(
        type: "totp"
    ) {
        secret
        uri
    }
}
mutation {
    accountCreateMFAAuthenticator(
        type: "totp"
    ) {
        secret
        uri
    }
}
```
