```graphql
mutation {
    teamsDeleteInstallation(
        teamId: "<TEAM_ID>",
        installationId: "<INSTALLATION_ID>"
    ) {
        status
    }
}
mutation {
    teamsDeleteInstallation(
        teamId: "<TEAM_ID>",
        installationId: "<INSTALLATION_ID>"
    ) {
        status
    }
}
```
