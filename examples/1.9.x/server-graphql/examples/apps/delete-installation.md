```graphql
mutation {
    appsDeleteInstallation(
        appId: "<APP_ID>",
        installationId: "<INSTALLATION_ID>"
    ) {
        status
    }
}
mutation {
    appsDeleteInstallation(
        appId: "<APP_ID>",
        installationId: "<INSTALLATION_ID>"
    ) {
        status
    }
}
```
