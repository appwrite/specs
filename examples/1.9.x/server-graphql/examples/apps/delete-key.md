```graphql
mutation {
    appsDeleteKey(
        appId: "<APP_ID>",
        keyId: "<KEY_ID>"
    ) {
        status
    }
}
mutation {
    appsDeleteKey(
        appId: "<APP_ID>",
        keyId: "<KEY_ID>"
    ) {
        status
    }
}
```
