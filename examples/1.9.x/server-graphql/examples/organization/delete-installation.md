```graphql
mutation {
    organizationDeleteInstallation(
        installationId: "<INSTALLATION_ID>"
    ) {
        status
    }
}
mutation {
    organizationDeleteInstallation(
        installationId: "<INSTALLATION_ID>"
    ) {
        status
    }
}
```
