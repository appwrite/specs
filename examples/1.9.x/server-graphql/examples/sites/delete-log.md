```graphql
mutation {
    sitesDeleteLog(
        siteId: "<SITE_ID>",
        logId: "<LOG_ID>"
    ) {
        status
    }
}
mutation {
    sitesDeleteLog(
        siteId: "<SITE_ID>",
        logId: "<LOG_ID>"
    ) {
        status
    }
}
```
