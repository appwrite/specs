```graphql
mutation {
    accountUpdateRecovery(
        userId: "<USER_ID>",
        secret: "<SECRET>",
        password: "password"
    ) {
        _id
        _createdAt
        userId
        secret
        expire
        phrase
    }
}
mutation {
    accountUpdateRecovery(
        userId: "<USER_ID>",
        secret: "<SECRET>",
        password: "password"
    ) {
        _id
        _createdAt
        userId
        secret
        expire
        phrase
    }
}
```
