```graphql
mutation {
    accountDeleteConsent(
        consentId: "<CONSENT_ID>"
    ) {
        status
    }
}
mutation {
    accountDeleteConsent(
        consentId: "<CONSENT_ID>"
    ) {
        status
    }
}
```
