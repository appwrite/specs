```graphql
mutation {
    accountDeleteConsentToken(
        consentId: "<CONSENT_ID>",
        tokenId: "<TOKEN_ID>"
    ) {
        status
    }
}
mutation {
    accountDeleteConsentToken(
        consentId: "<CONSENT_ID>",
        tokenId: "<TOKEN_ID>"
    ) {
        status
    }
}
```
