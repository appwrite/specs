```graphql
mutation {
    accountCreateMFAChallenge(
        factor: "email"
    ) {
        _id
        _createdAt
        userId
        expire
    }
}
mutation {
    accountCreateMFAChallenge(
        factor: "email"
    ) {
        _id
        _createdAt
        userId
        expire
    }
}
```
