```graphql
mutation {
    teamsUpdatePrefs(
        teamId: "<TEAM_ID>",
        prefs: "{}"
    ) {
        data
    }
}
mutation {
    teamsUpdatePrefs(
        teamId: "<TEAM_ID>",
        prefs: "{}"
    ) {
        data
    }
}
```
