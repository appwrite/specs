```graphql
mutation {
    oauth2Approve(
        grantId: "<GRANT_ID>",
        authorizationDetails: "<AUTHORIZATION_DETAILS>",
        scope: "<SCOPE>"
    ) {
        redirectUrl
    }
}
mutation {
    oauth2Approve(
        grantId: "<GRANT_ID>",
        authorizationDetails: "<AUTHORIZATION_DETAILS>",
        scope: "<SCOPE>"
    ) {
        redirectUrl
    }
}
```
