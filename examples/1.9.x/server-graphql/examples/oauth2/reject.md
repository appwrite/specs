```graphql
mutation {
    oauth2Reject(
        grantId: "<GRANT_ID>"
    ) {
        redirectUrl
    }
}
mutation {
    oauth2Reject(
        grantId: "<GRANT_ID>"
    ) {
        redirectUrl
    }
}
```
