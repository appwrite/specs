```graphql
mutation {
    oauth2Revoke(
        token: "<TOKEN>",
        tokenTypeHint: "access_token",
        clientId: "<CLIENT_ID>",
        clientSecret: "<CLIENT_SECRET>"
    ) {
        status
    }
}
mutation {
    oauth2Revoke(
        token: "<TOKEN>",
        tokenTypeHint: "access_token",
        clientId: "<CLIENT_ID>",
        clientSecret: "<CLIENT_SECRET>"
    ) {
        status
    }
}
```
